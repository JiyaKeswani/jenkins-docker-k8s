apiVersion: apps/v1
kind: Deployment
metadata:
  name: jiya-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jiya
      version: blue
  template:
    metadata:
      labels:
        app: jiya
        version: blue
    spec:
      containers:
      - name: jiya-container
        image: jiya2022/app:v2
        ports:
        - containerPort: 3000
